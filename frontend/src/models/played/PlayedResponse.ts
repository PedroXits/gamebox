export type PlayedResponse = {
  playedId: number;
  profileId: number;
  profileName: string;
  gameId: number;
  gameName: string;
  gamePhoto: string;
  bannerPhoto: string;
  createdAt: string;
};